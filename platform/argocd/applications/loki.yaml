apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: nexus-platform
    app.kubernetes.io/component: observability
  annotations:
    nexus.io/owner: platform-team
    nexus.io/adr: "ADR-007"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: nexus

  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: loki-stack
    targetRevision: "2.10.3"
    helm:
      values: |
        namespaceOverride: monitoring
        loki:
          enabled: true
          podAnnotations:
            nexus.io/autonomy-level: "0"
          persistence:
            enabled: false
          config:
            auth_enabled: false
            server:
              http_listen_port: 3100
            common:
              path_prefix: /loki
              storage:
                filesystem:
                  chunks_directory: /loki/chunks
                  rules_directory: /loki/rules
              replication_factor: 1
              ring:
                kvstore:
                  store: inmemory
            schema_config:
              configs:
                - from: 2020-10-24
                  store: boltdb-shipper
                  object_store: filesystem
                  schema: v11
                  index:
                    prefix: index_
                    period: 24h
          resources:
            limits:
              memory: 512Mi
              cpu: 300m
            requests:
              memory: 256Mi
              cpu: 150m
        promtail:
          enabled: true
          podAnnotations:
            nexus.io/autonomy-level: "0"
          config:
            clients:
              - url: http://loki.monitoring:3100/loki/api/v1/push
            snippets:
              pipelineStages:
                - cri: {}
              scrapeConfigs: |
                - job_name: kubernetes-pods
                  kubernetes_sd_configs:
                    - role: pod
                  relabel_configs:
                    - source_labels: [__meta_kubernetes_pod_node_name]
                      target_label: __host__
                    - action: labelmap
                      regex: __meta_kubernetes_pod_label_(.+)
                    - source_labels: [__meta_kubernetes_namespace]
                      target_label: namespace
                    - source_labels: [__meta_kubernetes_pod_name]
                      target_label: pod
                    - source_labels: [__meta_kubernetes_pod_container_name]
                      target_label: container
                  pipeline_stages:
                    - cri: {}
          resources:
            limits:
              memory: 256Mi
              cpu: 100m
            requests:
              memory: 128Mi
              cpu: 50m
        # Prevent loki-stack from auto-creating a datasource ConfigMap with
        # isDefault: true, which conflicts with Prometheus (also default) and
        # crashes Grafana ("Only one datasource per organization can be marked
        # as default"). Loki is added as isDefault: false via the observability
        # app's grafana.additionalDataSources instead.
        grafana:
          enabled: false
          sidecar:
            datasources:
              enabled: false

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
